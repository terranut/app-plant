import { ComponentStyleConfig } from '@chakra-ui/react';

const TagStyles: ComponentStyleConfig = {
  baseStyle: {
    size: 'md',
    borderRadius: 2,
    borderColor: 'primary.main',
    variant: 'solid',
    bg: '#F0F4FF',
    color: 'primary.main',
  },
};

export { TagStyles };
